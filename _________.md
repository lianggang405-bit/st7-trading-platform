# 前后端数据互通完整方案

## 项目架构

### 当前状态

#### 管理端
- ✅ 已实现完整的 CRUD API（50+ 接口）
- ✅ 使用 Supabase 作为数据源
- ❌ 环境变量配置错误：
  - 错误：`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - 正确：`COZE_SUPABASE_URL`, `COZE_SUPABASE_ANON_KEY`
- ❌ API 无法正常工作（返回 500 错误）

#### 前端
- ❌ 使用纯前端 Store（Zustand）
- ❌ 数据存储在内存中，刷新丢失
- ❌ 未对接任何后端 API
- ✅ 仅模拟账户功能实现了前后端互通（使用文件存储）

#### 数据库
- ✅ 已配置 Supabase（通过 Coze Workload Identity）
- ✅ PostgreSQL 配置：`DATABASE_URL`

## 数据互通架构

```
┌─────────────────────────────────────────────────────────────┐
│                  统一数据源 (Supabase)                        │
│  用户 | 订单 | 钱包 | KYC | 财务记录 | 交易对 | 系统配置     │
└─────────────────────────────────────────────────────────────┘
         ↑                                            ↑
         │ (REST API)                                 │ (REST API)
         │                                            │
┌─────────────────────┐                    ┌─────────────────────┐
│      前端应用        │                    │      管理后台        │
│  - 用户登录/注册     │◄────── 数据同步 ───►│  - 用户管理         │
│  - 交易下单         │                    │  - 订单管理         │
│  - 钱包充值提现     │                    │  - 钱包管理         │
│  - 资产管理         │                    │  - KYC 审核         │
│  - 实时行情         │                    │  - 财务记录         │
│  (调用 API 更新数据) │                    │  (调用 API 管理数据) │
└─────────────────────┘                    └─────────────────────┘
```

## 实施步骤

### 第一步：修复管理端 API 环境变量（紧急）

**问题**：50+ 管理端 API 使用了错误的环境变量

**解决方案**：
1. 批量替换所有 API 文件中的环境变量引用
2. 从 `NEXT_PUBLIC_SUPABASE_URL` 改为 `COZE_SUPABASE_URL`
3. 从 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 改为 `COZE_SUPABASE_ANON_KEY`

**影响文件**：50+ 个 API 路由文件

### 第二步：创建前端 API 层

**目标**：为前端所有功能创建统一的 API 调用层

**文件结构**：
```
src/api/
├── auth.ts              # 认证相关
├── user.ts              # 用户相关
├── wallet.ts            # 钱包相关
├── trading.ts           # 交易相关
├── order.ts             # 订单相关
├── kyc.ts               # KYC 相关
└── index.ts             # 统一导出
```

### 第三步：修改前端 Store 对接 API

**需要修改的 Store**：
1. `authStore.ts` - 已部分对接，继续完善
2. `assetStore.ts` - 对接钱包 API
3. `positionStore.ts` - 对接订单 API
4. `marketStore.ts` - 对接交易对 API
5. `stakingStore.ts` - 对接质押 API
6. `riskControlStore.ts` - 对接风控 API

### 第四步：实现数据同步机制

**方案 A：轮询（简单）**
```typescript
// 定期同步数据
useEffect(() => {
  const interval = setInterval(() => {
    fetchUserData();  // 同步用户数据
    fetchBalance();   // 同步余额
  }, 3000);  // 每3秒同步一次

  return () => clearInterval(interval);
}, []);
```

**方案 B：实时通知（推荐）**
```typescript
// 使用 WebSocket 或 Server-Sent Events
const eventSource = new EventSource('/api/events');
eventSource.onmessage = (event) => {
  const data = JSON.parse(event.data);
  updateStore(data.type, data.payload);
};
```

### 第五步：数据持久化

**方案**：
- 前端使用 localStorage 缓存关键数据
- 每次启动时从 API 同步最新数据
- 离线时使用缓存数据

## 数据模块清单

### 1. 用户模块
- 前端：注册、登录、个人信息
- 管理端：用户列表、用户详情、用户等级、实名管理
- API：`/api/auth/*`, `/api/admin/users/*`

### 2. 钱包模块
- 前端：余额、充值、提现、财务记录
- 管理端：充值申请、提现申请、财务记录、余额调整
- API：`/api/wallet/*`, `/api/admin/wallet/*`

### 3. 交易模块
- 前端：下单、平仓、持仓、历史订单
- 管理端：订单管理、交易对管理、杠杆配置
- API：`/api/trading/*`, `/api/admin/trading/*`, `/api/admin/contract/*`

### 4. KYC 模块
- 前端：实名认证申请
- 管理端：KYC 申请审核、用户详情
- API：`/api/kyc/*`, `/api/admin/users/kyc/*`

### 5. 系统配置
- 前端：交易对列表、交易时间、秒合约配置
- 管理端：交易对管理、交易时间管理、秒合约配置
- API：`/api/admin/trading/*`, `/api/admin/quick-contract/*`

## 关键技术点

### 1. 环境变量统一
```typescript
// 使用统一的 Supabase 客户端
import { getSupabaseClient } from '@/storage/database/supabase-client';

const supabase = getSupabaseClient();
```

### 2. API 错误处理
```typescript
// 统一错误处理
async function apiCall(url: string, options: RequestInit) {
  try {
    const response = await fetch(url, options);
    const data = await response.json();

    if (!data.success) {
      throw new Error(data.error || 'API 请求失败');
    }

    return data;
  } catch (error) {
    console.error('API 调用失败:', error);
    throw error;
  }
}
```

### 3. 数据缓存策略
```typescript
// 使用 localStorage 缓存
function withCache<T>(
  key: string,
  fetcher: () => Promise<T>,
  ttl: number = 60000  // 1分钟
): Promise<T> {
  const cached = localStorage.getItem(key);
  if (cached) {
    const { data, timestamp } = JSON.parse(cached);
    if (Date.now() - timestamp < ttl) {
      return Promise.resolve(data);
    }
  }

  return fetcher().then(data => {
    localStorage.setItem(key, JSON.stringify({
      data,
      timestamp: Date.now()
    }));
    return data;
  });
}
```

## 实施优先级

### P0 - 紧急（阻塞功能）
1. 修复管理端 API 环境变量
2. 测试管理端 API 是否正常工作

### P1 - 高优先级（核心功能）
1. 用户模块前后端对接
2. 钱包模块前后端对接
3. 订单模块前后端对接

### P2 - 中优先级（重要功能）
1. KYC 模块前后端对接
2. 财务记录前后端对接
3. 系统配置前后端对接

### P3 - 低优先级（优化功能）
1. 实时数据同步（WebSocket）
2. 数据缓存优化
3. 离线功能支持

## 验收标准

### 功能测试
- [ ] 前端注册的用户能在管理端看到
- [ ] 管理端修改用户状态，前端能实时体现
- [ ] 前端下单的订单能在管理端看到
- [ ] 管理端修改订单状态，前端能实时体现
- [ ] 前端充值申请能在管理端看到
- [ ] 管理端审核充值，前端余额能实时更新

### 性能测试
- [ ] API 响应时间 < 500ms
- [ ] 前端数据同步延迟 < 3s
- [ ] 页面加载时间 < 2s

### 稳定性测试
- [ ] 并发操作数据一致性
- [ ] 网络中断恢复机制
- [ ] 错误处理和降级方案

## 下一步行动

1. **立即行动**：修复管理端 API 环境变量
2. **短期目标**：完成用户和钱包模块前后端对接
3. **中期目标**：完成所有核心模块前后端对接
4. **长期目标**：实现实时数据同步和优化

---

**文档版本**: v1.0
**创建日期**: 2025-02-25
**更新日期**: 2025-02-25
